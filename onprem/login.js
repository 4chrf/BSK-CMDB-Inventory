const form=document.querySelector('#login');form.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button'),error=document.querySelector('#error');button.disabled=true;error.textContent='';try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const data=await response.json();if(!response.ok)throw Error(data.error);location.href=new URLSearchParams(location.search).get('return_to')==='/admin'?'/admin':'/'}catch(e){error.textContent=e.message;form.password.value='';button.disabled=false}});
